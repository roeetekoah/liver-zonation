# Organization review — repo structure

> **STATUS: EXECUTED.** The file moves (→ `docs/plan/`, `archive/`), the thin `src/README.md`,
> and all cross-reference fixes are done. Remaining for Claude Code (needs delete + git, which the
> assistant's sandbox can't do): delete junk (`src/__pycache__/`, `presentation/.~lock.*#`) and
> `git add -A && git commit`. The `git mv`/`git rm` move-list below is kept as the record of intent;
> the moves were done with plain `mv` (git will see them as rename on commit). One deviation:
> `IMPLEMENTATION_PLAN.tex`/`.pdf` were **kept** as the canonical pair (you read the PDF) and the
> redundant `.md` was retired to `archive/` — instead of the reverse.

Findings + a concrete reorg plan. The execution (file moves, deletes, git) is meant for
**Claude Code** (this sandbox can't move/delete tracked files or touch git). Nothing here
changes code behaviour — it's purely where things live and removing duplication.

## The core problem you flagged

Planning prose, typeset deliverables, and code are **coupled**: the same step-by-step content
exists as `src/CODING_PLAN.md`, `src/STEPS.md`, and `src/IMPLEMENTATION_PLAN.{md,tex,pdf}`, all
cross-referencing each other *and* the code. When code changes, 3–4 documents (and 3 file formats
of one of them) must change in lockstep or they drift. We've already hit that drift (stale
`classifier_step.py` reference in CODING_PLAN; stale "default = landmark" / "40 landmark feats"
fixed last round). One source of truth fixes this.

## Findings (by symptom)

**1. Four overlapping "how to build it" docs.**
- `src/STEPS.md` — plain-English per-step walkthrough.
- `src/CODING_PLAN.md` — build order + acceptance checks (A1–A9).
- `src/IMPLEMENTATION_PLAN.md` + `.tex` + `.pdf` — full spec (3 formats of the same content).
- `src/README.md` — points at all three.
They overlap heavily and all reference the code. → **Collapse to one canonical spec.**

**2. One document maintained in three formats.** `IMPLEMENTATION_PLAN.{md,tex,pdf}` must be kept
in sync by hand. The implementation plan is an *internal build doc*, not a graded deliverable
(those are the primer + deck). → **Keep `IMPLEMENTATION_PLAN.md` as the single living source; drop
the `.tex`/`.pdf`** (regenerate a PDF on demand only if you ever need to share one).

**3. Code and prose mixed in `src/`.** `src/` holds `*.py` *and* four `.md` planning docs. →
**`src/` keeps code + a thin usage `README.md`; all planning prose moves to `docs/plan/`.**

**4. Primer exists in 4 forms.** `Zonation_Primer.{tex,pdf}` (canonical) + `zonation_primer.html`
+ `zonation_v8.html` + `Spatial_Degradation_of_Hepatocyte_Zonation.pdf` (old renders). →
**Keep `Zonation_Primer.{tex,pdf}` + `primers/`; archive the rest.**

**5. Two decks.** `docs/Zonation_Hackathon_Deck.{pptx,pdf}` (old proposal deck) vs
`presentation/Zonation_Final_Presentation.{pptx,pdf}` (current). → **`presentation/` is canonical;
archive the `docs/` deck.**

**6. Duplicated asset.** `results/figures/p2_validation.png` == `presentation/assets/p2_validation.png`.
→ keep one (the `results/` one is a pipeline output; the `presentation/` copy is the asset). Leave
both but know `results/figures/` is regenerated by the pipeline.

**7. Junk to delete.** `src/__pycache__/`, `presentation/.~lock.*#` (LibreOffice locks),
any `~$*` Office locks, scattered LaTeX `.aux/.log/.out/.toc`.

**8. Stale internal references** to fix during the move: `CODING_PLAN.md` → `classifier_step.py`
(should be `classifier.py`); any links pointing at `src/IMPLEMENTATION_PLAN.*` after it moves.

## Proposed target layout

```
Hackathon/
├── README.md              ← NEW short top-level: what this is + links (replaces scattered entry points)
├── PROJECT_MAP.md         ← stays: the index
├── CLAUDE.md              ← stays: agent context
├── FEASIBILITY_AND_PREP.md← stays: 2-day plan + advance prep
├── data/  signatures/  results/  papers/      ← unchanged
├── src/                   ← CODE ONLY
│   ├── README.md          ← thin: how to run, points to docs/plan/
│   ├── config.py pipeline.py classifier.py run_all.py run_p2_validation.py
│   ├── prep/  steps/  plotting/
│   └── (no .md planning docs here)
├── docs/
│   ├── plan/              ← NEW: all build/planning prose, one source each
│   │   ├── IMPLEMENTATION_PLAN.md   ← THE spec (md only; absorbs STEPS + CODING_PLAN)
│   │   ├── WORK_DIVISION.md
│   │   └── ORIGINAL_PLAN_AND_CHANGES.md
│   ├── Zonation_Primer.{tex,pdf}    ← canonical primer
│   ├── primers/                     ← the 3 method primers
│   └── research_question_options.md
├── presentation/         ← canonical deck + assets (PRESENTATION_PLAN.md stays here)
└── archive/              ← NEW: superseded artefacts, out of the way but not lost
    ├── zonation_primer.html  zonation_v8.html
    ├── Spatial_Degradation_of_Hepatocyte_Zonation.pdf
    └── Zonation_Hackathon_Deck.{pptx,pdf}
```

## Concrete move-list (for Claude Code to execute with `git mv` / `git rm`)

1. `git mv src/IMPLEMENTATION_PLAN.md docs/plan/IMPLEMENTATION_PLAN.md`
2. Fold the unique content of `src/STEPS.md` (the "why start at Step 2" intro) and
   `src/CODING_PLAN.md` (the A1–A9 acceptance checks) **into** `docs/plan/IMPLEMENTATION_PLAN.md`,
   then `git rm src/STEPS.md src/CODING_PLAN.md`.
3. `git rm src/IMPLEMENTATION_PLAN.tex src/IMPLEMENTATION_PLAN.pdf` (md is now the source).
4. `git mv docs/WORK_DIVISION.md docs/ORIGINAL_PLAN_AND_CHANGES.md docs/plan/`
5. `mkdir archive && git mv docs/zonation_primer.html docs/zonation_v8.html
   docs/Spatial_Degradation_of_Hepatocyte_Zonation.pdf
   docs/Zonation_Hackathon_Deck.pptx docs/Zonation_Hackathon_Deck.pdf archive/`
6. `git rm -r src/__pycache__` and delete `presentation/.~lock.*#`; add `__pycache__/`,
   `*.aux *.log *.out *.toc`, `.~lock.*#`, `~$*` to `.gitignore` if not already.
7. Rewrite the thin `src/README.md` to point at `docs/plan/IMPLEMENTATION_PLAN.md`.
8. Fix references after moving: `CODING_PLAN`→(removed), `classifier_step.py`→`classifier.py`,
   and every link to `src/IMPLEMENTATION_PLAN.*` → `docs/plan/IMPLEMENTATION_PLAN.md`
   (in `PROJECT_MAP.md`, `CLAUDE.md`, `src/README.md`, `signatures/README.md`).
9. Commit as one "repo reorg: one source of truth for the plan; code/prose separation".

> Decision left to you: dropping `IMPLEMENTATION_PLAN.tex/.pdf` (step 3) assumes the plan is an
> internal doc. If you want a typeset PDF to hand the professor, keep the `.tex` as the source and
> drop the `.md` instead — but don't maintain both.
